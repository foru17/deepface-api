# This file is kept for users / hosting platforms that expect a flat
# requirements.txt. The canonical dependency source is `pyproject.toml`.
# Regenerate with: `pip install pip-tools && pip-compile pyproject.toml`.
#
# Runtime dependencies only — install dev / test extras via:
#     pip install -e ".[dev,test]"

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
numpy>=1.24,<3
opencv-python-headless>=4.8,<5
deepface>=0.0.93,<0.1
retina-face>=0.0.14,<0.1
tensorflow>=2.13,<3; platform_machine != 'arm64' or sys_platform != 'darwin'
tensorflow-macos>=2.13,<2.16; platform_machine == 'arm64' and sys_platform == 'darwin'
# Keras 2 compatibility for retinaface / deepface on TF >= 2.16.
tf-keras>=2.16; platform_machine != 'arm64' or sys_platform != 'darwin'
